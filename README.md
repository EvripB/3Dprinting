# RPI commands

## WiFi
`iwgetid` to view to which WiFi SSID you are connected

`iwconfig wlan0 | grep Quality` to view WiFi signal strength

`/etc/wpa_supplicant/wpa_supplicant.conf` the location of WiFi configuration